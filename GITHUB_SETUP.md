# Инструкция по настройке GitHub

## Быстрая настройка

### Вариант 1: Использовать скрипт (Windows)

Запустите файл `setup_github.bat` и следуйте инструкциям:

```bash
setup_github.bat
```

### Вариант 2: Ручная настройка

1. **Создайте репозиторий на GitHub** (если еще не создан):
   - Перейдите на https://github.com/new
   - Имя репозитория: `telegram-ref-bot` (или другое)
   - **НЕ** добавляйте README, .gitignore или лицензию
   - Нажмите "Create repository"

2. **Добавьте remote репозиторий:**

   **Через HTTPS:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/telegram-ref-bot.git
   ```

   **Через SSH:**
   ```bash
   git remote add origin git@github.com:ВАШ_USERNAME/telegram-ref-bot.git
   ```

3. **Проверьте remote:**
   ```bash
   git remote -v
   ```

4. **Отправьте изменения:**
   ```bash
   git push -u origin main
   ```

## Если репозиторий уже существует

Если remote уже настроен, но нужно изменить URL:

```bash
# Удалить старый remote
git remote remove origin

# Добавить новый
git remote add origin https://github.com/ВАШ_USERNAME/telegram-ref-bot.git

# Отправить изменения
git push -u origin main
```

## Аутентификация

### HTTPS
При использовании HTTPS вам понадобится:
- Personal Access Token (PAT) вместо пароля
- Создайте токен: https://github.com/settings/tokens
- Используйте токен как пароль при push

### SSH
При использовании SSH:
- Убедитесь, что SSH ключ добавлен в GitHub
- Проверьте: `ssh -T git@github.com`

## Проверка

После настройки проверьте:

```bash
# Проверить remote
git remote -v

# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline -5
```

## Текущий статус

✅ Локальный Git бэкап выполнен  
✅ Все изменения закоммичены  
⏳ GitHub remote требует настройки  
📦 Готово к отправке после настройки remote

